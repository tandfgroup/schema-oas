Review:
  allOf:
    - $ref: 'creative_work.yml#/CreativeWork'
    - type: object
      description: A review of an item - for example, of a restaurant, movie, or store.
      properties:
        author:
          $ref: 'person.yml#/Person'
